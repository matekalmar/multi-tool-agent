[project]
name = "multi-tool-agent"
version = "0.1.0"
description = "Modular AI agent with PDF search, CSV analysis and calculator tools"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "langchain>=1.0.0",
    "langchain-community>=0.4.0",
    "langchain-openai>=1.0.0",
    "langchain-text-splitters>=1.0.0",
    "pandas>=2.0.0",
    "pypdf>=5.0.0",
    "python-dotenv>=1.0.0",
]